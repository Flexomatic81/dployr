<!-- Page Header -->
<% const canManage = projectAccess.isOwner || projectAccess.permission === 'manage' || projectAccess.permission === 'full'; %>
<div class="d-page-header">
    <div class="d-page-header-left">
        <a href="/projects" class="btn btn-sm btn-subtle mb-2">
            <i class="bi bi-arrow-left"></i> <%= t('common:nav.projects') %>
        </a>
        <h1 class="d-page-title">
            <i class="bi bi-folder"></i>
            <%= project.name %>
        </h1>
        <div class="d-flex align-items-center gap-2 mt-2">
            <% if (project.status === 'running') { %>
                <span class="badge bg-success-subtle text-success"><i class="bi bi-check-circle-fill"></i> <%= t('common:status.running') %></span>
            <% } else { %>
                <span class="badge bg-secondary-subtle text-secondary"><i class="bi bi-stop-circle"></i> <%= t('common:status.stopped') %></span>
            <% } %>
            <% if (!projectAccess.isOwner) { %>
                <span class="badge bg-info-subtle text-info" title="<%= t('projects:show.sharedProject') %> <%= t('projects:show.sharedFrom') %> <%= projectAccess.ownerUsername %>">
                    <i class="bi bi-share"></i> <%= t('projects:show.shared') %>
                </span>
            <% } %>
        </div>
    </div>
    <div class="d-page-header-actions">
        <a href="/logs/<%= project.name %>" class="btn btn-outline-secondary">
            <i class="bi bi-terminal"></i> <%= t('common:buttons.logs') %>
        </a>
        <% if (project.status === 'running') { %>
            <% if (canManage) { %>
            <form action="/projects/<%= project.name %>/restart" method="POST" class="d-inline">
                <button type="submit" class="btn btn-outline-warning">
                    <i class="bi bi-arrow-clockwise"></i> <%= t('common:buttons.restart') %>
                </button>
            </form>
            <form action="/projects/<%= project.name %>/stop" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger">
                    <i class="bi bi-stop-fill"></i> <%= t('common:buttons.stop') %>
                </button>
            </form>
            <% } %>
        <% } else { %>
            <% if (canManage) { %>
            <form action="/projects/<%= project.name %>/start" method="POST" class="d-inline">
                <button type="submit" class="btn btn-success">
                    <i class="bi bi-play-fill"></i> <%= t('common:buttons.start') %>
                </button>
            </form>
            <% } %>
        <% } %>
        <% if (projectAccess.isOwner) { %>
        <!-- Owner actions dropdown -->
        <div class="dropdown d-inline-block">
            <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-three-dots-vertical"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <button type="button" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#cloneModal">
                        <i class="bi bi-copy me-2"></i> <%= t('projects:clone.action') %>
                    </button>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li>
                    <button type="button" class="dropdown-item text-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                        <i class="bi bi-trash me-2"></i> <%= t('common:buttons.delete') %>
                    </button>
                </li>
            </ul>
        </div>
        <% } %>
    </div>
</div>

<% if (!projectAccess.isOwner) { %>
<!-- Shared project info banner -->
<div class="alert alert-info d-elevated mb-4">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <i class="bi bi-share-fill me-2"></i>
            <strong><%= t('projects:show.sharedProject') %></strong> <%= t('projects:show.sharedFrom') %> <strong><%= projectAccess.ownerUsername %></strong>
        </div>
        <span class="badge <%= projectAccess.permission === 'full' ? 'bg-success-subtle text-success' : projectAccess.permission === 'manage' ? 'bg-primary-subtle text-primary' : 'bg-secondary-subtle text-secondary' %>">
            <i class="bi <%= projectAccess.permission === 'full' ? 'bi-star' : projectAccess.permission === 'manage' ? 'bi-gear' : 'bi-eye' %>"></i>
            <%= permissionLabels[projectAccess.permission] %>
        </span>
    </div>
</div>
<% } %>

<div class="row">
    <!-- Project info -->
    <div class="col-md-6 mb-4">
        <div class="card d-elevated h-100">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> <%= t('projects:details') %></h5>
            </div>
            <div class="card-body">
                <table class="table table-borderless mb-0">
                    <tr>
                        <th width="35%"><%= t('projects:info.name') %>:</th>
                        <td><%= project.name %></td>
                    </tr>
                    <tr>
                        <th><%= t('projects:info.type') %>:</th>
                        <td>
                            <% if (project.templateType === 'php-website') { %>
                                <span class="badge bg-purple-subtle text-purple"><%= t('projects:types.php') %></span>
                            <% } else if (project.templateType === 'nodejs-app') { %>
                                <span class="badge bg-success-subtle text-success"><%= t('projects:types.nodejs') %></span>
                            <% } else if (project.templateType === 'laravel') { %>
                                <span class="badge bg-danger-subtle text-danger"><%= t('projects:types.laravel') %></span>
                            <% } else if (project.templateType === 'nodejs-static') { %>
                                <span class="badge bg-info-subtle text-info"><%= t('projects:types.nodejsStatic') %></span>
                            <% } else if (project.templateType === 'nextjs') { %>
                                <span class="badge bg-dark text-white"><%= t('projects:types.nextjs') %></span>
                            <% } else if (project.templateType === 'custom') { %>
                                <span class="badge bg-warning-subtle text-warning"><i class="bi bi-gear-wide-connected"></i> <%= t('projects:types.custom') %></span>
                            <% } else { %>
                                <span class="badge bg-secondary-subtle text-secondary"><%= t('projects:types.static') %></span>
                            <% } %>
                        </td>
                    </tr>
                    <tr>
                        <th><%= t('projects:info.port') %>:</th>
                        <td>
                            <a href="http://<%= serverIp %>:<%= project.port %>" target="_blank" class="text-decoration-none">
                                <%= project.port %> <i class="bi bi-box-arrow-up-right"></i>
                            </a>
                        </td>
                    </tr>
                    <tr>
                        <th><%= t('projects:info.path') %>:</th>
                        <td><code class="small"><%= project.path %></code></td>
                    </tr>
                    <tr>
                        <th><%= t('projects:info.containerName') %>:</th>
                        <td><code class="small"><%= project.containerName %></code></td>
                    </tr>
                    <% if (project.hasDatabase) { %>
                        <tr>
                            <th><%= t('projects:info.database') %>:</th>
                            <td><code class="small"><%= project.database %></code></td>
                        </tr>
                    <% } %>
                </table>
            </div>
        </div>
    </div>

    <!-- Container status -->
    <div class="col-md-6 mb-4">
        <div class="card d-elevated h-100">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="bi bi-box"></i>
                    <% if (project.isCustom) { %>
                        <%= t('projects:services.title') %>
                    <% } else { %>
                        <%= t('projects:info.containers') %>
                    <% } %>
                </h5>
                <div class="d-flex align-items-center gap-2">
                    <span class="badge bg-secondary-subtle text-secondary">
                        <%= project.runningContainers %>/<%= project.totalContainers %> <%= t('common:status.active').toLowerCase() %>
                    </span>
                    <% if (project.isCustom && canManage) { %>
                        <form action="/projects/<%= project.name %>/rebuild" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-outline-warning" title="<%= t('projects:services.rebuildHelp') %>">
                                <i class="bi bi-hammer"></i> <%= t('projects:services.rebuild') %>
                            </button>
                        </form>
                    <% } %>
                </div>
            </div>
            <div class="card-body">
                <% if (project.containers.length === 0) { %>
                    <%- include('../components/emptyState', {
                        icon: 'bi-box-seam',
                        title: t('projects:info.noContainers'),
                        description: t('projects:info.noContainersHint'),
                        compact: true
                    }) %>
                <% } else if (project.isCustom && project.services && project.services.length > 0) { %>
                    <!-- Multi-service display for custom docker-compose projects -->
                    <div class="list-group list-group-flush">
                        <% project.services.forEach(service => { %>
                            <div class="list-group-item px-0">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <i class="bi bi-box<%= service.State === 'running' ? '-fill text-success' : ' text-muted' %>"></i>
                                        <strong><%= service.Service || service.Name %></strong>
                                        <% if (service.State === 'running') { %>
                                            <span class="badge bg-success-subtle text-success ms-2"><%= t('projects:services.running') %></span>
                                        <% } else { %>
                                            <span class="badge bg-secondary-subtle text-secondary ms-2"><%= service.State || t('projects:services.stopped') %></span>
                                        <% } %>
                                        <br>
                                        <small class="text-muted">
                                            <% if (service.Image) { %><%= service.Image %><% } %>
                                            <% if (service.Ports) { %> &bull; <%= service.Ports %><% } %>
                                        </small>
                                    </div>
                                    <% if (canManage) { %>
                                    <div class="btn-group btn-group-sm">
                                        <button type="button" class="btn btn-outline-secondary"
                                                onclick="showServiceLogs('<%= service.Service || service.Name %>')"
                                                title="<%= t('projects:services.logs') %>">
                                            <i class="bi bi-terminal"></i>
                                        </button>
                                        <form action="/projects/<%= project.name %>/services/<%= service.Service || service.Name %>/restart" method="POST" class="d-inline">
                                            <button type="submit" class="btn btn-outline-warning" title="<%= t('projects:services.restart') %>">
                                                <i class="bi bi-arrow-clockwise"></i>
                                            </button>
                                        </form>
                                    </div>
                                    <% } %>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <!-- Standard container display -->
                    <div class="list-group list-group-flush">
                        <% project.containers.forEach(container => { %>
                            <div class="list-group-item d-flex justify-content-between align-items-center px-0">
                                <div>
                                    <i class="bi bi-box<%= container.State === 'running' ? '-fill text-success' : ' text-muted' %>"></i>
                                    <strong><%= container.Names[0].replace('/', '') %></strong>
                                    <br>
                                    <small class="text-muted"><%= container.Image %></small>
                                </div>
                                <% if (container.State === 'running') { %>
                                    <span class="badge bg-success-subtle text-success"><%= t('common:status.running') %></span>
                                <% } else { %>
                                    <span class="badge bg-secondary-subtle text-secondary"><%= container.State %></span>
                                <% } %>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

<!-- Service Logs Modal (for custom docker-compose projects) -->
<% if (project.isCustom && canManage) { %>
<div class="modal fade" id="serviceLogsModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="bi bi-terminal"></i> <span id="serviceLogsTitle">Service Logs</span>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <pre id="serviceLogsContent" class="bg-dark text-light p-3 rounded" style="max-height: 500px; overflow-y: auto; font-size: 0.8rem;"></pre>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:buttons.close') %></button>
                <button type="button" class="btn btn-primary" id="refreshLogsBtn">
                    <i class="bi bi-arrow-clockwise"></i> <%= t('common:buttons.refresh') %>
                </button>
            </div>
        </div>
    </div>
</div>

<script>
let currentServiceName = '';

function showServiceLogs(serviceName) {
    currentServiceName = serviceName;
    document.getElementById('serviceLogsTitle').textContent = serviceName + ' Logs';
    document.getElementById('serviceLogsContent').textContent = 'Loading...';

    const modal = new bootstrap.Modal(document.getElementById('serviceLogsModal'));
    modal.show();

    fetchServiceLogs(serviceName);
}

function fetchServiceLogs(serviceName) {
    fetch(`/projects/<%= project.name %>/services/${serviceName}/logs?lines=200`)
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                document.getElementById('serviceLogsContent').textContent = data.logs || 'No logs available';
            } else {
                document.getElementById('serviceLogsContent').textContent = 'Error: ' + data.error;
            }
        })
        .catch(error => {
            document.getElementById('serviceLogsContent').textContent = 'Error: ' + error.message;
        });
}

document.getElementById('refreshLogsBtn')?.addEventListener('click', function() {
    if (currentServiceName) {
        fetchServiceLogs(currentServiceName);
    }
});
</script>
<% } %>

<!-- Development Workspace (only for manage permission or higher) -->
<% if (canManage) { %>
<div class="card d-elevated mb-4">
    <div class="card-header">
        <h5 class="mb-0">
            <i class="bi bi-code-square"></i> <%= t('workspaces:title') %>
            <%- include('../partials/help-icon', { text: t('workspaces:help') }) %>
        </h5>
    </div>
    <div class="card-body">
        <% if (workspace) { %>
            <!-- Workspace exists -->
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <% if (workspace.status === 'running') { %>
                        <span class="badge bg-success-subtle text-success">
                            <i class="bi bi-circle-fill"></i> <%= t('workspaces:status.running') %>
                        </span>
                        <small class="text-muted ms-2">
                            <%= t('workspaces:runningSince') %>: <%= new Date(workspace.started_at).toLocaleString(currentLanguage === 'de' ? 'de-DE' : 'en-US') %>
                        </small>
                    <% } else if (workspace.status === 'starting') { %>
                        <span class="badge bg-info-subtle text-info">
                            <i class="bi bi-hourglass-split"></i> <%= t('workspaces:status.starting') %>
                        </span>
                    <% } else if (workspace.status === 'stopping') { %>
                        <span class="badge bg-warning-subtle text-warning">
                            <i class="bi bi-hourglass-split"></i> <%= t('workspaces:status.stopping') %>
                        </span>
                    <% } else if (workspace.status === 'error') { %>
                        <span class="badge bg-danger-subtle text-danger">
                            <i class="bi bi-exclamation-triangle"></i> <%= t('workspaces:status.error') %>
                        </span>
                        <% if (workspace.error_message) { %>
                            <small class="text-danger ms-2"><%= workspace.error_message %></small>
                        <% } %>
                    <% } else { %>
                        <span class="badge bg-secondary-subtle text-secondary">
                            <i class="bi bi-stop-circle"></i> <%= t('workspaces:status.stopped') %>
                        </span>
                    <% } %>
                </div>
                <div class="btn-group">
                    <% if (workspace.status === 'running') { %>
                        <a href="/workspaces/<%= project.name %>/ide" class="btn btn-primary" target="_blank" title="<%= t('workspaces:actions.openIDE') %>">
                            <i class="bi bi-box-arrow-up-right"></i> <%= t('workspaces:actions.openIDE') %>
                        </a>
                        <form action="/workspaces/<%= project.name %>/stop" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-warning" title="<%= t('workspaces:actions.stop') %>">
                                <i class="bi bi-stop-fill"></i> <%= t('workspaces:actions.stop') %>
                            </button>
                        </form>
                    <% } else if (workspace.status === 'stopped' || workspace.status === 'error') { %>
                        <form action="/workspaces/<%= project.name %>/start" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-success" title="<%= t('workspaces:actions.start') %>">
                                <i class="bi bi-play-fill"></i> <%= t('workspaces:actions.start') %>
                            </button>
                        </form>
                    <% } %>
                    <% if (workspace.status !== 'starting' && workspace.status !== 'stopping') { %>
                        <div class="btn-group">
                            <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-three-dots-vertical"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li>
                                    <a class="dropdown-item" href="/workspaces/<%= project.name %>">
                                        <i class="bi bi-gear me-2"></i> <%= t('workspaces:actions.settings') %>
                                    </a>
                                </li>
                                <% if (workspace.status === 'running') { %>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <form action="/workspaces/<%= project.name %>/sync/to-project" method="POST">
                                        <button type="submit" class="dropdown-item">
                                            <i class="bi bi-arrow-down-circle me-2"></i> <%= t('workspaces:actions.syncToProject') %>
                                        </button>
                                    </form>
                                </li>
                                <li>
                                    <form action="/workspaces/<%= project.name %>/sync/from-project" method="POST">
                                        <button type="submit" class="dropdown-item">
                                            <i class="bi bi-arrow-up-circle me-2"></i> <%= t('workspaces:actions.syncFromProject') %>
                                        </button>
                                    </form>
                                </li>
                                <% } %>
                                <% if (projectAccess.isOwner) { %>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <button type="button" class="dropdown-item text-danger" data-bs-toggle="modal" data-bs-target="#deleteWorkspaceModal">
                                        <i class="bi bi-trash me-2"></i> <%= t('workspaces:actions.delete') %>
                                    </button>
                                </li>
                                <% } %>
                            </ul>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Resource info -->
            <% if (workspace.status === 'running') { %>
            <div class="row text-center">
                <div class="col-4">
                    <small class="text-muted"><%= t('workspaces:resources.cpu') %></small>
                    <div><strong><%= workspace.cpu_limit %></strong></div>
                </div>
                <div class="col-4">
                    <small class="text-muted"><%= t('workspaces:resources.ram') %></small>
                    <div><strong><%= workspace.ram_limit %></strong></div>
                </div>
                <div class="col-4">
                    <small class="text-muted"><%= t('workspaces:resources.port') %></small>
                    <div><strong><%= workspace.assigned_port %></strong></div>
                </div>
            </div>
            <% } %>
        <% } else { %>
            <!-- No workspace -->
            <div class="text-center py-3">
                <i class="bi bi-code-square fs-1 text-muted"></i>
                <p class="text-muted mt-2 mb-3"><%= t('workspaces:noWorkspace') %></p>
                <form action="/workspaces/<%= project.name %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> <%= t('workspaces:create.button') %>
                    </button>
                </form>
            </div>
        <% } %>
    </div>
</div>
<% } %>

<!-- Delete Workspace Modal (only for owner with workspace) -->
<% if (projectAccess.isOwner && workspace) { %>
<div class="modal fade" id="deleteWorkspaceModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">
                    <i class="bi bi-exclamation-triangle"></i> <%= t('workspaces:delete.confirmTitle') %>
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p><%- t('workspaces:delete.confirmText', { project: project.name }) %></p>
                <div class="alert alert-danger">
                    <i class="bi bi-exclamation-triangle"></i>
                    <strong><%= t('common:confirm.deleteWarning') %></strong>
                </div>
                <ul>
                    <li><%= t('workspaces:delete.warning1') %></li>
                    <li><%= t('workspaces:delete.warning2') %></li>
                    <li><strong><%= t('workspaces:delete.warning3') %></strong></li>
                </ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:buttons.cancel') %></button>
                <form action="/workspaces/<%= project.name %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> <%= t('common:buttons.delete') %>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
<% } %>

<% if (gitStatus && gitStatus.connected) { %>
<!-- Git - combined Git integration and Auto-Deploy -->
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseGit" aria-expanded="false" aria-controls="collapseGit">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span>
                <i class="bi bi-git"></i> <%= t('projects:git.title') %>
                <span class="badge bg-success-subtle text-success ms-2"><%= t('projects:git.connected') %></span>
                <% if (autoDeployConfig && autoDeployConfig.enabled) { %>
                    <span class="badge bg-info-subtle text-info ms-1"><%= t('projects:autoDeploy.title') %></span>
                <% } %>
                <% if (webhookConfig && webhookConfig.webhook_enabled) { %>
                    <span class="badge bg-purple-subtle text-purple ms-1"><i class="bi bi-webhook"></i> Webhook</span>
                <% } %>
            </span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseGit">
    <div class="card-body">
        <% if (gitStatus.error) { %>
            <div class="alert alert-warning mb-0">
                <i class="bi bi-exclamation-triangle"></i> <%= gitStatus.error %>
            </div>
        <% } else { %>
            <!-- Repository info -->
            <div class="row mb-3">
                <div class="col-md-8">
                    <table class="table table-borderless table-sm mb-0">
                        <tr>
                            <th width="20%"><%= t('projects:git.repository') %>:</th>
                            <td>
                                <a href="<%= gitStatus.remoteUrl %>" target="_blank" class="text-decoration-none">
                                    <%= gitStatus.remoteUrl %> <i class="bi bi-box-arrow-up-right"></i>
                                </a>
                            </td>
                        </tr>
                        <tr>
                            <th><%= t('projects:git.branch') %>:</th>
                            <td><span class="badge bg-primary-subtle text-primary"><%= gitStatus.branch %></span></td>
                        </tr>
                        <tr>
                            <th><%= t('projects:git.lastCommit') %>:</th>
                            <td><code class="small"><%= gitStatus.lastCommit %></code></td>
                        </tr>
                        <% if (gitStatus.hasLocalChanges) { %>
                            <tr>
                                <th><%= t('common:labels.status') %>:</th>
                                <td><span class="badge bg-warning-subtle text-warning"><%= t('projects:info.localChanges') %></span></td>
                            </tr>
                        <% } %>
                    </table>
                </div>
                <div class="col-md-4 d-flex align-items-start justify-content-end">
                    <div class="btn-group">
                        <% if (canManage) { %>
                        <form action="/projects/<%= project.name %>/git/pull" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-primary btn-sm">
                                <i class="bi bi-cloud-download"></i> <%= t('projects:git.pull') %>
                            </button>
                        </form>
                        <form action="/projects/<%= project.name %>/autodeploy/trigger" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-outline-primary btn-sm" title="Pull + Restart">
                                <i class="bi bi-lightning"></i> Deploy
                            </button>
                        </form>
                        <% } %>
                        <% if (projectAccess.isOwner) { %>
                        <button type="button" class="btn btn-outline-danger btn-sm" data-bs-toggle="modal" data-bs-target="#gitDisconnectModal" title="<%= t('projects:show.gitDisconnectTitle') %>">
                            <i class="bi bi-x-circle"></i>
                        </button>
                        <% } %>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Auto-Deploy section (only for owner) -->
        <% if (projectAccess.isOwner) { %>
        <div class="border-top pt-3">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div class="d-flex align-items-center gap-2">
                    <strong><i class="bi bi-arrow-repeat"></i> <%= t('projects:autoDeploy.title') %></strong>
                    <%- include('../partials/help-icon', { text: t('projects:show.autoDeployHelp') }) %>
                    <% if (autoDeployConfig && autoDeployConfig.enabled) { %>
                        <span class="badge bg-success-subtle text-success"><%= t('projects:show.autoDeployActive') %></span>
                        <form action="/projects/<%= project.name %>/autodeploy/interval" method="POST" class="d-inline" id="intervalForm">
                            <select name="interval" class="form-select form-select-sm" style="width: auto; display: inline-block;" id="intervalSelect"
                                data-bs-toggle="tooltip" data-bs-placement="top" title="<%= t('projects:show.intervalTooltip') %>">
                                <% const currentInterval = autoDeployConfig.interval_minutes || 5; %>
                                <option value="5" <%= currentInterval === 5 ? 'selected' : '' %>>5 Min</option>
                                <option value="10" <%= currentInterval === 10 ? 'selected' : '' %>>10 Min</option>
                                <option value="15" <%= currentInterval === 15 ? 'selected' : '' %>>15 Min</option>
                                <option value="30" <%= currentInterval === 30 ? 'selected' : '' %>>30 Min</option>
                                <option value="60" <%= currentInterval === 60 ? 'selected' : '' %>>60 Min</option>
                            </select>
                        </form>
                    <% } else { %>
                        <span class="badge bg-secondary-subtle text-secondary"><%= t('projects:show.autoDeployInactive') %></span>
                    <% } %>
                </div>
                <div>
                    <% if (autoDeployConfig && autoDeployConfig.enabled) { %>
                        <form action="/projects/<%= project.name %>/autodeploy/disable" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-pause-circle"></i> <%= t('projects:autoDeploy.disable') %>
                            </button>
                        </form>
                    <% } else { %>
                        <form action="/projects/<%= project.name %>/autodeploy/enable" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-success btn-sm">
                                <i class="bi bi-play-circle"></i> <%= t('projects:autoDeploy.enable') %>
                            </button>
                        </form>
                    <% } %>
                </div>
            </div>

            <% if (autoDeployConfig && autoDeployConfig.enabled) { %>
                <% if (autoDeployConfig.last_check) { %>
                    <p class="text-muted small mb-3">
                        <i class="bi bi-clock"></i> <%= t('projects:autoDeploy.lastCheck') %>: <%= new Date(autoDeployConfig.last_check).toLocaleString(currentLanguage === 'de' ? 'de-DE' : 'en-US') %>
                    </p>
                <% } %>

                <% if (deploymentHistory && deploymentHistory.length > 0) { %>
                    <h6 class="mb-2"><%= t('projects:show.deployHistory') %></h6>
                    <div class="table-responsive">
                        <table class="table table-sm table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th><%= t('admin:logs.timestamp') %></th>
                                    <th><%= t('projects:show.deployType') %></th>
                                    <th><%= t('projects:show.deployCommit') %></th>
                                    <th><%= t('projects:show.deployStatus') %></th>
                                    <th><%= t('projects:show.deployDuration') %></th>
                                </tr>
                            </thead>
                            <tbody>
                                <% deploymentHistory.forEach(log => { %>
                                    <tr>
                                        <td>
                                            <small><%= new Date(log.created_at).toLocaleString(currentLanguage === 'de' ? 'de-DE' : 'en-US') %></small>
                                        </td>
                                        <td>
                                            <% if (log.trigger_type === 'auto') { %>
                                                <span class="badge bg-info-subtle text-info"><%= t('projects:show.deployAuto') %></span>
                                            <% } else { %>
                                                <span class="badge bg-secondary-subtle text-secondary"><%= t('projects:show.deployManual') %></span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (log.old_commit_hash && log.new_commit_hash && log.old_commit_hash !== log.new_commit_hash) { %>
                                                <code class="small"><%= log.old_commit_hash.substring(0, 7) %></code>
                                                <i class="bi bi-arrow-right text-muted"></i>
                                                <code class="small"><%= log.new_commit_hash.substring(0, 7) %></code>
                                            <% } else if (log.new_commit_hash) { %>
                                                <code class="small"><%= log.new_commit_hash.substring(0, 7) %></code>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                            <% if (log.commit_message && log.commit_message !== t('projects:show.noChanges')) { %>
                                                <br><small class="text-muted" title="<%= log.commit_message %>">
                                                    <%= log.commit_message.length > 40 ? log.commit_message.substring(0, 40) + '...' : log.commit_message %>
                                                </small>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (log.status === 'success') { %>
                                                <span class="badge bg-success-subtle text-success"><%= t('common:status.success') %></span>
                                            <% } else if (log.status === 'failed') { %>
                                                <span class="badge bg-danger-subtle text-danger" title="<%= log.error_message %>"><%= t('common:status.failed') %></span>
                                            <% } else if (log.status === 'pulling') { %>
                                                <span class="badge bg-info-subtle text-info">Pulling...</span>
                                            <% } else if (log.status === 'restarting') { %>
                                                <span class="badge bg-warning-subtle text-warning"><%= t('common:buttons.restart') %>...</span>
                                            <% } else { %>
                                                <span class="badge bg-secondary-subtle text-secondary"><%= log.status %></span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (log.duration_ms) { %>
                                                <small><%= (log.duration_ms / 1000).toFixed(1) %>s</small>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="text-muted text-center py-2">
                        <i class="bi bi-clock-history"></i> <%= t('projects:show.noDeploymentsYet') %>
                    </div>
                <% } %>
            <% } else { %>
                <p class="text-muted small mb-0">
                    <i class="bi bi-info-circle"></i>
                    <%= t('projects:show.autoDeployInfo') %>
                </p>
            <% } %>
        </div>

        <!-- Webhook section (independent of polling auto-deploy) -->
        <div class="border-top pt-3 mt-3">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div class="d-flex align-items-center gap-2">
                    <strong><i class="bi bi-webhook"></i> Webhook</strong>
                    <%- include('../partials/help-icon', { text: t('projects:webhook.help') }) %>
                    <% if (webhookConfig && webhookConfig.webhook_enabled) { %>
                        <span class="badge bg-success-subtle text-success"><%= t('common:status.active') %></span>
                    <% } else { %>
                        <span class="badge bg-secondary-subtle text-secondary"><%= t('common:status.inactive') %></span>
                    <% } %>
                </div>
                <div>
                    <% if (webhookConfig && webhookConfig.webhook_enabled) { %>
                        <form action="/projects/<%= project.name %>/webhook/disable" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-pause-circle"></i> <%= t('projects:webhook.disable') %>
                            </button>
                        </form>
                    <% } else { %>
                        <form action="/projects/<%= project.name %>/webhook/enable" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-success btn-sm">
                                <i class="bi bi-play-circle"></i> <%= t('projects:webhook.enable') %>
                            </button>
                        </form>
                    <% } %>
                </div>
            </div>

            <% if (webhookConfig && webhookConfig.webhook_enabled) { %>
                <!-- Webhook URL -->
                <div class="mb-3">
                    <label class="form-label small text-muted"><%= t('projects:webhook.urlLabel') %></label>
                    <div class="input-group input-group-sm">
                        <span class="input-group-text"><i class="bi bi-link-45deg"></i></span>
                        <input type="text" class="form-control font-monospace" id="webhookUrl"
                               value="<%= serverIp.includes('localhost') ? 'http' : 'https' %>://<%= serverIp %>/api/webhooks/<%= webhookConfig.id %>"
                               readonly>
                        <button class="btn btn-outline-secondary copy-btn" type="button" data-copy-target="webhookUrl" title="<%= t('common:buttons.copy') %>">
                            <i class="bi bi-clipboard"></i>
                        </button>
                    </div>
                </div>

                <!-- One-time secret display (after enable/regenerate) -->
                <% if (webhookSecret && webhookSecret.secret) { %>
                <div class="alert alert-warning mb-3">
                    <div class="d-flex align-items-start">
                        <i class="bi bi-key-fill fs-4 me-3"></i>
                        <div class="flex-grow-1">
                            <strong><%= t('projects:webhook.secretLabel') %></strong>
                            <p class="small mb-2"><%= t('projects:webhook.secretWarning') %></p>
                            <div class="input-group input-group-sm">
                                <input type="text" class="form-control font-monospace" id="webhookSecret"
                                       value="<%= webhookSecret.secret %>" readonly>
                                <button class="btn btn-outline-secondary copy-btn" type="button" data-copy-target="webhookSecret" title="<%= t('common:buttons.copy') %>">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>

                <!-- Regenerate secret button -->
                <div class="d-flex justify-content-between align-items-center">
                    <small class="text-muted">
                        <i class="bi bi-info-circle"></i>
                        <%= t('projects:webhook.branchInfo', { branch: webhookConfig.branch || 'main' }) %>
                    </small>
                    <form action="/projects/<%= project.name %>/webhook/regenerate" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-warning btn-sm"
                                data-confirm="<%= t('projects:webhook.regenerateConfirm') %>">
                            <i class="bi bi-arrow-repeat"></i> <%= t('projects:webhook.regenerate') %>
                        </button>
                    </form>
                </div>

                <!-- Setup instructions (collapsible) -->
                <div class="mt-3">
                    <a class="text-decoration-none small" data-bs-toggle="collapse" href="#webhookSetup" role="button" aria-expanded="false">
                        <i class="bi bi-question-circle"></i> <%= t('projects:webhook.setupInstructions') %>
                    </a>
                    <div class="collapse mt-2" id="webhookSetup">
                        <div class="card card-body bg-body-secondary small">
                            <div class="mb-3">
                                <strong><i class="bi bi-github"></i> GitHub:</strong>
                                <ol class="mb-0 ps-3 mt-1">
                                    <li><%= t('projects:webhook.github.step1') %></li>
                                    <li><%= t('projects:webhook.github.step2') %></li>
                                    <li><%= t('projects:webhook.github.step3') %></li>
                                    <li><%= t('projects:webhook.github.step4') %></li>
                                </ol>
                            </div>
                            <div class="mb-3">
                                <strong><i class="bi bi-gitlab text-warning"></i> GitLab:</strong>
                                <ol class="mb-0 ps-3 mt-1">
                                    <li><%= t('projects:webhook.gitlab.step1') %></li>
                                    <li><%= t('projects:webhook.gitlab.step2') %></li>
                                    <li><%= t('projects:webhook.gitlab.step3') %></li>
                                </ol>
                            </div>
                            <div>
                                <strong><i class="bi bi-bucket text-primary"></i> Bitbucket:</strong>
                                <ol class="mb-0 ps-3 mt-1">
                                    <li><%= t('projects:webhook.bitbucket.step1') %></li>
                                    <li><%= t('projects:webhook.bitbucket.step2') %></li>
                                    <li><%= t('projects:webhook.bitbucket.step3') %></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <p class="text-muted small mb-0">
                    <i class="bi bi-info-circle"></i>
                    <%= t('projects:webhook.info') %>
                </p>
            <% } %>
        </div>
        <% } %><!-- End projectAccess.isOwner for Auto-Deploy -->
    </div>
    </div><!-- End collapse -->
</div>
<% } %>

<% const canChangeType = projectAccess.isOwner || projectAccess.permission === 'full'; %>
<!-- Project type warning on mismatch (only for owner/full, NOT for custom projects) -->
<% if (typeMismatch && canChangeType && !project.isCustom) { %>
<div class="alert alert-warning d-elevated mb-4" role="alert">
    <div class="d-flex align-items-center">
        <i class="bi bi-exclamation-triangle-fill fs-4 me-3"></i>
        <div class="flex-grow-1">
            <strong><%= t('projects:show.typeRecommendation') %></strong> <%= t('projects:show.typeMismatchInfo') %>
            <br>
            <small>
                <%= t('projects:show.currentType') %>: <strong>
                <% if (project.templateType === 'php-website') { %><%= t('projects:types.php') %>
                <% } else if (project.templateType === 'nodejs-app') { %><%= t('projects:types.nodejs') %>
                <% } else if (project.templateType === 'laravel') { %><%= t('projects:types.laravel') %>
                <% } else if (project.templateType === 'nodejs-static') { %><%= t('projects:types.nodejsStatic') %>
                <% } else if (project.templateType === 'nextjs') { %><%= t('projects:types.nextjs') %>
                <% } else { %><%= t('projects:types.static') %><% } %>
                </strong>
                &rarr; <%= t('projects:show.recommendedType') %>: <strong>
                <% if (detectedType === 'php') { %><%= t('projects:types.php') %>
                <% } else if (detectedType === 'nodejs') { %><%= t('projects:types.nodejs') %>
                <% } else if (detectedType === 'laravel') { %><%= t('projects:types.laravel') %>
                <% } else if (detectedType === 'nodejs-static') { %><%= t('projects:types.nodejsStatic') %>
                <% } else if (detectedType === 'nextjs') { %><%= t('projects:types.nextjs') %>
                <% } else { %><%= t('projects:types.static') %><% } %>
                </strong>
            </small>
        </div>
        <form action="/projects/<%= project.name %>/change-type" method="POST" class="ms-3">
            <input type="hidden" name="type" value="<%= detectedType %>">
            <button type="submit" class="btn btn-warning btn-sm">
                <i class="bi bi-arrow-repeat"></i> <%= t('projects:show.adjustType') %>
            </button>
        </form>
    </div>
</div>
<% } %>

<!-- Environment variables (only for manage or higher) -->
<% if (canManage) { %>
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0 flex-grow-1" role="button" data-bs-toggle="collapse" data-bs-target="#collapseEnv" aria-expanded="false" aria-controls="collapseEnv">
                <i class="bi bi-file-earmark-code"></i> <%= t('projects:show.envTitle') %>
                <%- include('../partials/help-icon', { text: t('projects:show.envHelp') }) %>
                <% if (userDatabases && userDatabases.length > 0) { %>
                <small class="text-muted ms-2">& <%= t('projects:show.dbConnection') %></small>
                <% } %>
            </h5>
            <div class="d-flex align-items-center">
                <% if (userDatabases && userDatabases.length > 0) { %>
                <div class="btn-group btn-group-sm me-2">
                    <button type="button" class="btn btn-outline-primary dropdown-toggle" data-bs-toggle="dropdown" data-bs-auto-close="true"
                        title="<%= envExample && envExample.exists ? t('projects:show.setupDbHelp', { filename: envExample.filename }) : t('projects:show.setupDbHelpNoExample') %>">
                        <i class="bi bi-database-add"></i> <%= t('projects:show.setupDb') %>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <% if (envExample && envExample.exists) { %>
                        <li class="dropdown-header">
                            <i class="bi bi-file-earmark-code"></i> <%= t('projects:show.usesTemplate', { filename: envExample.filename }) %>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <% } %>
                        <% userDatabases.forEach(db => { %>
                        <li>
                            <form action="/projects/<%= project.name %>/env/add-db" method="POST" class="db-setup-form">
                                <input type="hidden" name="database" value="<%= db.database %>">
                                <button type="submit" class="dropdown-item">
                                    <i class="bi bi-database<%= db.type === 'postgresql' ? '-fill text-primary' : ' text-warning' %>"></i>
                                    <%= db.name %> <small class="text-muted">(<%= db.type %>)</small>
                                </button>
                            </form>
                        </li>
                        <% }) %>
                    </ul>
                </div>
                <% } %>
                <i class="bi bi-chevron-down collapse-icon" role="button" data-bs-toggle="collapse" data-bs-target="#collapseEnv" aria-expanded="false" aria-controls="collapseEnv"></i>
            </div>
        </div>
    </div>
    <div class="collapse" id="collapseEnv">
    <div class="card-body">
        <% if (envExample && envExample.exists && !envContent.trim() && userDatabases && userDatabases.length > 0) { %>
        <div class="alert alert-info mb-3">
            <i class="bi bi-lightbulb"></i>
            <strong><%= t('projects:env.exampleFound') %>:</strong> <%= t('projects:show.envTip', { filename: envExample.filename }) %>
            <%= t('projects:show.envTipDb') %>
        </div>
        <% } else if (envExample && envExample.exists && !envContent.trim()) { %>
        <div class="alert alert-info mb-3">
            <i class="bi bi-lightbulb"></i>
            <strong><%= t('projects:env.exampleFound') %>:</strong> <%= t('projects:show.envTip', { filename: envExample.filename }) %>
            <form action="/projects/<%= project.name %>/env/copy-example" method="POST" class="d-inline">
                <button type="submit" class="btn btn-sm btn-outline-info ms-2">
                    <i class="bi bi-file-earmark-arrow-down"></i> <%= t('projects:show.useAsTemplate') %>
                </button>
            </form>
        </div>
        <% } %>
        <form action="/projects/<%= project.name %>/env" method="POST">
            <div class="mb-3">
                <textarea name="envContent" class="form-control font-monospace" rows="10"
                    placeholder="<%= t('projects:show.envPlaceholder') %>"><%= envContent %></textarea>
            </div>
            <div class="d-flex justify-content-between align-items-center">
                <small class="text-muted">
                    <i class="bi bi-info-circle"></i>
                    <%= t('projects:show.envRestartHint') %>
                </small>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-save"></i> <%= t('common:buttons.save') %>
                </button>
            </div>
        </form>
    </div>
    </div><!-- End collapse -->
</div>
<% } %><!-- End canManage for environment variables -->

<!-- Project settings (only for owner/full, NOT for custom projects) -->
<% if (canChangeType && !project.isCustom) { %>
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseSettings" aria-expanded="false" aria-controls="collapseSettings">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span><i class="bi bi-gear"></i> <%= t('projects:settings') %></span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseSettings">
    <div class="card-body">
        <form action="/projects/<%= project.name %>/change-type" method="POST" class="row g-3 align-items-end">
            <div class="col-md-6">
                <label for="projectType" class="form-label">
                    <%= t('projects:show.projectType') %>
                    <%- include('../partials/help-icon', { text: t('projects:show.projectTypeHelp') }) %>
                </label>
                <select name="type" id="projectType" class="form-select">
                    <optgroup label="<%= t('projects:templateInfo.static.title') %>">
                        <option value="static" <%= project.templateType === 'static-website' ? 'selected' : '' %>>
                            <%= t('projects:types.static') %> (nginx)
                        </option>
                        <option value="php" <%= project.templateType === 'php-website' ? 'selected' : '' %>>
                            <%= t('projects:types.php') %> (Apache + PHP)
                        </option>
                        <option value="nodejs" <%= project.templateType === 'nodejs-app' ? 'selected' : '' %>>
                            <%= t('projects:types.nodejs') %> (Express)
                        </option>
                    </optgroup>
                    <optgroup label="Framework">
                        <option value="laravel" <%= project.templateType === 'laravel' ? 'selected' : '' %>>
                            <%= t('projects:types.laravel') %> (Composer)
                        </option>
                        <option value="nodejs-static" <%= project.templateType === 'nodejs-static' ? 'selected' : '' %>>
                            <%= t('projects:types.nodejsStatic') %>
                        </option>
                        <option value="nextjs" <%= project.templateType === 'nextjs' ? 'selected' : '' %>>
                            <%= t('projects:types.nextjs') %>
                        </option>
                    </optgroup>
                </select>
            </div>
            <div class="col-md-6">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-arrow-repeat"></i> <%= t('projects:typeChange.changeType') %>
                </button>
            </div>
            <div class="col-12">
                <% if (!typeMismatch) { %>
                <small class="text-success">
                    <i class="bi bi-check-circle"></i>
                    <%= t('projects:show.typeMatchOk') %>
                </small>
                <% } else { %>
                <small class="text-muted">
                    <i class="bi bi-info-circle"></i>
                    <%= t('projects:show.typeChangeInfo') %>
                </small>
                <% } %>
            </div>
        </form>
    </div>
    </div><!-- End collapse -->
</div>
<% } else if (canChangeType && project.isCustom) { %>
<!-- Custom project info - type change not supported -->
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseSettings" aria-expanded="false" aria-controls="collapseSettings">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span><i class="bi bi-gear"></i> <%= t('projects:settings') %></span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseSettings">
    <div class="card-body">
        <div class="alert alert-info mb-0">
            <i class="bi bi-info-circle"></i>
            <strong><%= t('projects:types.custom') %></strong>:
            <%= t('projects:customProject.info') %>
        </div>
    </div>
    </div><!-- End collapse -->
</div>
<% } %><!-- End canChangeType for project settings -->

<!-- Share project (only for owner) -->
<% if (projectAccess.isOwner) { %>
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseShare" aria-expanded="false" aria-controls="collapseShare">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span><i class="bi bi-share"></i> <%= t('projects:sharing.title') %></span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseShare">
    <div class="card-body">
        <% if (projectShares && projectShares.length > 0) { %>
        <h6 class="mb-3"><%= t('projects:sharing.sharedWith') %>:</h6>
        <div class="table-responsive mb-3">
            <table class="table table-sm table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th><%= t('common:labels.user') %></th>
                        <th><%= t('common:labels.permission') %></th>
                        <th class="text-end"><%= t('common:labels.actions') %></th>
                    </tr>
                </thead>
                <tbody>
                    <% projectShares.forEach(share => { %>
                    <tr>
                        <td>
                            <i class="bi bi-person"></i>
                            <strong><%= share.username %></strong>
                            <small class="text-muted">(<%= share.shared_system_username %>)</small>
                        </td>
                        <td>
                            <form action="/projects/<%= project.name %>/shares/<%= share.shared_with_id %>/update" method="POST" class="d-inline share-permission-form">
                                <select name="permission" class="form-select form-select-sm share-permission-select" style="width: auto;">
                                    <option value="read" <%= share.permission === 'read' ? 'selected' : '' %>><%= t('projects:sharing.permissions.read') %></option>
                                    <option value="manage" <%= share.permission === 'manage' ? 'selected' : '' %>><%= t('projects:sharing.permissions.manage') %></option>
                                    <option value="full" <%= share.permission === 'full' ? 'selected' : '' %>><%= t('projects:sharing.permissions.full') %></option>
                                </select>
                            </form>
                        </td>
                        <td class="text-end">
                            <form action="/projects/<%= project.name %>/shares/<%= share.shared_with_id %>/delete" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-outline-danger btn-sm" data-confirm="<%= t('projects:sharing.removeConfirm', { username: share.username }) %>">
                                    <i class="bi bi-x-lg"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <% } else { %>
        <p class="text-muted mb-3">
            <i class="bi bi-info-circle"></i>
            <%= t('projects:show.sharedWithNone') %>
        </p>
        <% } %>

        <% if (availableUsers && availableUsers.length > 0) { %>
        <hr>
        <h6 class="mb-3"><%= t('projects:show.addNewUser') %></h6>
        <form action="/projects/<%= project.name %>/shares" method="POST" class="row g-2 align-items-end">
            <div class="col-md-5">
                <label for="shareUser" class="form-label"><%= t('common:labels.user') %></label>
                <select name="userId" id="shareUser" class="form-select" required>
                    <option value=""><%= t('projects:show.selectUser') %></option>
                    <% availableUsers.forEach(user => { %>
                    <option value="<%= user.id %>"><%= user.username %> (<%= user.system_username %>)</option>
                    <% }) %>
                </select>
            </div>
            <div class="col-md-4">
                <label for="sharePermission" class="form-label">
                    <%= t('common:labels.permission') %>
                    <%- include('../partials/help-icon', { text: t('projects:sharing.permissionHelp') }) %>
                </label>
                <select name="permission" id="sharePermission" class="form-select" required>
                    <option value="read"><%= t('projects:sharing.permissions.read') %></option>
                    <option value="manage" selected><%= t('projects:sharing.permissions.manage') %></option>
                    <option value="full"><%= t('projects:sharing.permissions.full') %></option>
                </select>
            </div>
            <div class="col-md-3">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-plus-lg"></i> <%= t('common:buttons.add') %>
                </button>
            </div>
        </form>
        <div class="mt-2">
            <small class="text-muted">
                <%= t('projects:show.permissionLabels') %>
            </small>
        </div>
        <% } else if (projectShares && projectShares.length === 0) { %>
        <p class="text-muted small mb-0">
            <i class="bi bi-info-circle"></i>
            <%= t('projects:show.noUsersToShare') %>
        </p>
        <% } %>
    </div>
    </div><!-- End collapse -->
</div>
<% } %>

<!-- Domain Management (only if NPM enabled and owner/full permission) -->
<% if (npmEnabled && (projectAccess.isOwner || projectAccess.permission === 'full')) { %>
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseDomains" aria-expanded="false" aria-controls="collapseDomains">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span>
                <i class="bi bi-globe2"></i> <%= t('proxy:title') %>
                <% if (projectDomains && projectDomains.length > 0) { %>
                    <span class="badge bg-success-subtle text-success ms-2"><%= projectDomains.length %></span>
                <% } %>
            </span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseDomains">
    <div class="card-body">
        <% if (projectDomains && projectDomains.length > 0) { %>
        <div class="table-responsive mb-3">
            <table class="table table-sm table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th><%= t('proxy:domain') %></th>
                        <th><%= t('proxy:ssl') %></th>
                        <th class="text-end"><%= t('common:labels.actions') %></th>
                    </tr>
                </thead>
                <tbody>
                    <% projectDomains.forEach(domain => { %>
                    <tr>
                        <td>
                            <a href="<%= domain.ssl_enabled ? 'https' : 'http' %>://<%= domain.domain %>" target="_blank" class="text-decoration-none">
                                <i class="bi bi-box-arrow-up-right"></i> <%= domain.domain %>
                            </a>
                        </td>
                        <td>
                            <% if (domain.ssl_enabled) { %>
                                <span class="badge bg-success-subtle text-success">
                                    <i class="bi bi-lock-fill"></i> <%= t('proxy:sslActive') %>
                                </span>
                            <% } else { %>
                                <form action="/proxy/<%= project.name %>/domains/<%= domain.domain %>/ssl" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-outline-warning btn-sm">
                                        <i class="bi bi-shield-lock"></i> <%= t('proxy:enableSsl') %>
                                    </button>
                                </form>
                            <% } %>
                        </td>
                        <td class="text-end">
                            <form action="/proxy/<%= project.name %>/domains/<%= domain.domain %>?_method=DELETE" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-outline-danger btn-sm btn-icon" data-confirm="<%= t('proxy:confirmDelete') %>">
                                    <i class="bi bi-x-lg"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <% } else { %>
        <p class="text-muted mb-3">
            <i class="bi bi-info-circle"></i> <%= t('proxy:noDomains') %>
        </p>
        <% } %>

        <hr>
        <h6 class="mb-3"><%= t('proxy:addDomain') %></h6>
        <form action="/proxy/<%= project.name %>/domains" method="POST" class="row g-2 align-items-end">
            <div class="col-md-5">
                <label for="domain" class="form-label"><%= t('proxy:domainLabel') %></label>
                <input type="text" class="form-control" id="domain" name="domain"
                       placeholder="example.com" required
                       pattern="[a-zA-Z0-9][a-zA-Z0-9-_.]*\.[a-zA-Z]{2,}">
                <div class="form-text"><%= t('proxy:domainHelp') %></div>
            </div>
            <div class="col-md-4">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="enableSsl" name="enableSsl" value="true" checked>
                    <label class="form-check-label" for="enableSsl">
                        <i class="bi bi-shield-lock"></i> <%= t('proxy:requestSsl') %>
                    </label>
                </div>
            </div>
            <div class="col-md-3">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-plus-lg"></i> <%= t('common:buttons.add') %>
                </button>
            </div>
        </form>
    </div>
    </div><!-- End collapse -->
</div>
<% } %>

<!-- Backup Card (for manage permission or higher) -->
<% if (canManage) { %>
<%
    const hasProjectBackups = typeof projectBackups !== 'undefined' && projectBackups && projectBackups.length > 0;
    const hasDatabaseBackups = typeof databaseBackups !== 'undefined' && databaseBackups && databaseBackups.length > 0;
    const totalBackups = (hasProjectBackups ? projectBackups.length : 0) + (hasDatabaseBackups ? databaseBackups.length : 0);
%>
<div class="card d-elevated mb-4">
    <div class="card-header d-collapsible-header collapsed" role="button" data-bs-toggle="collapse" data-bs-target="#collapseBackups" aria-expanded="false" aria-controls="collapseBackups">
        <h5 class="mb-0 d-flex justify-content-between align-items-center">
            <span>
                <i class="bi bi-archive"></i> <%= t('backups:projectCard.title') %>
                <% if (totalBackups > 0) { %>
                    <span class="badge bg-primary-subtle text-primary ms-2"><%= totalBackups %></span>
                <% } %>
            </span>
            <i class="bi bi-chevron-down collapse-icon"></i>
        </h5>
    </div>
    <div class="collapse" id="collapseBackups">
    <div class="card-body">
        <% if (hasProjectBackups || hasDatabaseBackups) { %>
        <div class="table-responsive mb-3">
            <table class="table table-sm table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th><%= t('backups:table.type') %></th>
                        <th><%= t('backups:table.name') %></th>
                        <th><%= t('backups:table.date') %></th>
                        <th><%= t('backups:table.size') %></th>
                        <th><%= t('backups:table.status') %></th>
                        <th class="text-end"><%= t('backups:table.actions') %></th>
                    </tr>
                </thead>
                <tbody>
                    <% if (hasProjectBackups) { %>
                    <% projectBackups.forEach(backup => { %>
                    <tr>
                        <td>
                            <span class="badge bg-primary-subtle text-primary">
                                <i class="bi bi-folder"></i> <%= t('backups:types.project') %>
                            </span>
                        </td>
                        <td><%= project.name %></td>
                        <td>
                            <small><%= new Date(backup.created_at).toLocaleString(currentLanguage === 'de' ? 'de-DE' : 'en-GB') %></small>
                        </td>
                        <td>
                            <% if (backup.file_size) { %>
                                <%= typeof formatFileSize === 'function' ? formatFileSize(backup.file_size) : Math.round(backup.file_size / 1024) + ' KB' %>
                            <% } else { %>
                                <span class="text-muted">-</span>
                            <% } %>
                        </td>
                        <td>
                            <% if (backup.status === 'success') { %>
                                <span class="badge bg-success-subtle text-success"><i class="bi bi-check-circle"></i></span>
                            <% } else if (backup.status === 'failed') { %>
                                <span class="badge bg-danger-subtle text-danger"><i class="bi bi-x-circle"></i></span>
                            <% } else { %>
                                <span class="badge bg-warning-subtle text-warning"><i class="bi bi-hourglass-split"></i></span>
                            <% } %>
                        </td>
                        <td class="text-end">
                            <a href="/backups/<%= backup.id %>" class="btn btn-sm btn-outline-secondary btn-icon" title="<%= t('backups:detail.title') %>">
                                <i class="bi bi-eye"></i>
                            </a>
                            <% if (backup.status === 'success') { %>
                            <a href="/backups/<%= backup.id %>/download" class="btn btn-sm btn-outline-primary btn-icon" title="<%= t('backups:download') %>">
                                <i class="bi bi-download"></i>
                            </a>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                    <% } %>
                    <% if (hasDatabaseBackups) { %>
                    <% databaseBackups.forEach(backup => { %>
                    <tr>
                        <td>
                            <span class="badge bg-info-subtle text-info">
                                <i class="bi bi-database"></i> <%= t('backups:types.database') %>
                            </span>
                        </td>
                        <td><%= backup.target_name %></td>
                        <td>
                            <small><%= new Date(backup.created_at).toLocaleString(currentLanguage === 'de' ? 'de-DE' : 'en-GB') %></small>
                        </td>
                        <td>
                            <% if (backup.file_size) { %>
                                <%= typeof formatFileSize === 'function' ? formatFileSize(backup.file_size) : Math.round(backup.file_size / 1024) + ' KB' %>
                            <% } else { %>
                                <span class="text-muted">-</span>
                            <% } %>
                        </td>
                        <td>
                            <% if (backup.status === 'success') { %>
                                <span class="badge bg-success-subtle text-success"><i class="bi bi-check-circle"></i></span>
                            <% } else if (backup.status === 'failed') { %>
                                <span class="badge bg-danger-subtle text-danger"><i class="bi bi-x-circle"></i></span>
                            <% } else { %>
                                <span class="badge bg-warning-subtle text-warning"><i class="bi bi-hourglass-split"></i></span>
                            <% } %>
                        </td>
                        <td class="text-end">
                            <a href="/backups/<%= backup.id %>" class="btn btn-sm btn-outline-secondary btn-icon" title="<%= t('backups:detail.title') %>">
                                <i class="bi bi-eye"></i>
                            </a>
                            <% if (backup.status === 'success') { %>
                            <a href="/backups/<%= backup.id %>/download" class="btn btn-sm btn-outline-primary btn-icon" title="<%= t('backups:download') %>">
                                <i class="bi bi-download"></i>
                            </a>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
        <% } else { %>
        <p class="text-muted mb-3">
            <i class="bi bi-info-circle"></i> <%= t('backups:projectCard.never') %>
        </p>
        <% } %>

        <div class="d-flex flex-wrap gap-2 justify-content-between align-items-center">
            <div class="d-flex flex-wrap gap-2">
                <!-- Project Backup Button -->
                <form action="/backups/project/<%= project.name %>?returnTo=/projects/<%= project.name %>" method="POST" class="d-inline" id="backupForm">
                    <%- csrfInput %>
                    <button type="submit" class="btn btn-primary" id="createBackupBtn">
                        <i class="bi bi-folder"></i> <%= t('backups:projectCard.backupProject') %>
                    </button>
                </form>

                <!-- Database Backup Button (only if linked database exists) -->
                <% if (typeof linkedDatabase !== 'undefined' && linkedDatabase) { %>
                <form action="/backups/database/<%= linkedDatabase.database %>?returnTo=/projects/<%= project.name %>" method="POST" class="d-inline">
                    <%- csrfInput %>
                    <button type="submit" class="btn btn-outline-primary" title="<%= linkedDatabase.database %> (<%= linkedDatabase.type %>)">
                        <i class="bi bi-database"></i> <%= t('backups:projectCard.backupDatabase') %>
                    </button>
                </form>
                <% } %>
            </div>
            <a href="/backups" class="btn btn-link btn-sm">
                <%= t('backups:projectCard.viewAll') %> <i class="bi bi-arrow-right"></i>
            </a>
        </div>
    </div>
    </div><!-- End collapse -->
</div>
<% } %>

<!-- Delete Modal (only for owner) -->
<% if (projectAccess.isOwner) { %>
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">
                    <i class="bi bi-exclamation-triangle"></i> <%= t('projects:show.deleteConfirmTitle') %>
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p><%- t('projects:show.deleteConfirmText', { name: project.name }) %></p>
                <div class="alert alert-danger">
                    <i class="bi bi-exclamation-triangle"></i>
                    <strong><%= t('common:confirm.deleteWarning') %></strong>
                </div>
                <ul>
                    <li><%= t('projects:show.deleteConfirmList1') %></li>
                    <li><%= t('projects:show.deleteConfirmList2') %></li>
                    <li><strong><%= t('projects:show.deleteConfirmList3') %></strong></li>
                </ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:buttons.cancel') %></button>
                <form action="/projects/<%= project.name %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> <%= t('projects:show.confirmDelete') %>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
<% } %><!-- End projectAccess.isOwner for Delete Modal -->

<!-- Git Disconnect Modal (only for owner) -->
<% if (projectAccess.isOwner) { %>
<div class="modal fade" id="gitDisconnectModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-warning">
                <h5 class="modal-title">
                    <i class="bi bi-x-circle"></i> <%= t('projects:show.gitDisconnectTitle') %>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p><%= t('projects:show.gitDisconnectConfirm') %></p>
                <div class="alert alert-warning">
                    <i class="bi bi-exclamation-triangle"></i>
                    <strong><%= t('common:confirm.deleteWarning').replace('!', ':') %></strong> <%= t('projects:show.gitDisconnectNote') %>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:buttons.cancel') %></button>
                <form action="/projects/<%= project.name %>/git/disconnect" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-x-circle"></i> <%= t('projects:show.disconnect') %>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
<% } %><!-- End projectAccess.isOwner for Git Disconnect Modal -->

<!-- Clone Modal (only for owner) -->
<% if (projectAccess.isOwner) { %>
<div class="modal fade" id="cloneModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="bi bi-copy"></i> <%= t('projects:clone.title') %>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/projects/<%= project.name %>/clone" method="POST">
                <div class="modal-body">
                    <p class="text-muted"><%= t('projects:clone.modalDescription') %></p>
                    <div class="mb-3">
                        <label for="cloneNewName" class="form-label"><%= t('projects:clone.newName') %></label>
                        <input type="text" class="form-control" id="cloneNewName" name="newName"
                               pattern="[a-z0-9-]+" required
                               placeholder="<%= project.name %>-copy"
                               value="<%= project.name %>-copy">
                        <div class="form-text"><%= t('projects:create.projectNameHint') %></div>
                    </div>
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle"></i>
                        <strong><%= t('projects:clone.infoTitle') %>:</strong>
                        <ul class="mb-0 mt-2">
                            <li><%= t('projects:clone.info1') %></li>
                            <li><%= t('projects:clone.info2') %></li>
                            <li><%= t('projects:clone.info3') %></li>
                        </ul>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common:buttons.cancel') %></button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-copy"></i> <%= t('projects:clone.button') %>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
<% } %><!-- End projectAccess.isOwner for Clone Modal -->

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Copy to clipboard buttons
    document.querySelectorAll('.copy-btn').forEach(function(btn) {
        btn.addEventListener('click', function() {
            const targetId = this.dataset.copyTarget;
            const input = document.getElementById(targetId);
            if (input) {
                input.select();
                input.setSelectionRange(0, 99999); // For mobile
                navigator.clipboard.writeText(input.value).then(function() {
                    // Visual feedback
                    const icon = btn.querySelector('i');
                    if (icon) {
                        icon.className = 'bi bi-check';
                        setTimeout(function() { icon.className = 'bi bi-clipboard'; }, 1500);
                    }
                });
            }
        });
    });
    // Auto-submit interval form on change
    const intervalSelect = document.getElementById('intervalSelect');
    if (intervalSelect) {
        intervalSelect.addEventListener('change', function() {
            document.getElementById('intervalForm').submit();
        });
    }

    // Auto-submit share permission forms on change
    document.querySelectorAll('.share-permission-select').forEach(function(select) {
        select.addEventListener('change', function() {
            this.closest('form').submit();
        });
    });

    // Confirm dialogs for delete buttons
    document.querySelectorAll('[data-confirm]').forEach(function(btn) {
        btn.addEventListener('click', function(e) {
            if (!confirm(this.dataset.confirm)) {
                e.preventDefault();
            }
        });
    });

    // Sync header 'collapsed' class with Bootstrap collapse state for arrow animation
    document.querySelectorAll('.collapse').forEach(function(collapseEl) {
        const card = collapseEl.closest('.card');
        const header = card ? card.querySelector('.d-collapsible-header') : null;
        if (header) {
            collapseEl.addEventListener('show.bs.collapse', function() {
                header.classList.remove('collapsed');
            });
            collapseEl.addEventListener('hide.bs.collapse', function() {
                header.classList.add('collapsed');
            });
        }
    });

    // Keep collapse cards open after form submit
    // Find all forms inside collapse cards and remember which card to reopen
    document.querySelectorAll('.collapse').forEach(function(collapseEl) {
        const forms = collapseEl.querySelectorAll('form');
        forms.forEach(function(form) {
            form.addEventListener('submit', function() {
                sessionStorage.setItem('openCollapse', collapseEl.id);
            });
        });
    });

    // Handle db-setup-form in header dropdown (not inside collapse body)
    // Use mousedown on button to capture before dropdown closes
    document.querySelectorAll('.db-setup-form button[type="submit"]').forEach(function(btn) {
        btn.addEventListener('mousedown', function() {
            // Find the associated collapse for this card
            const card = btn.closest('.card');
            const collapseEl = card ? card.querySelector('.collapse') : null;
            if (collapseEl) {
                sessionStorage.setItem('openCollapse', collapseEl.id);
            }
        });
    });

    // Also handle auto-submit selects - save collapse state before submit
    document.querySelectorAll('.share-permission-select').forEach(function(select) {
        const collapseEl = select.closest('.collapse');
        if (collapseEl) {
            select.addEventListener('change', function() {
                sessionStorage.setItem('openCollapse', collapseEl.id);
            });
        }
    });

    // Interval select - reuse existing reference
    if (intervalSelect) {
        const collapseEl = intervalSelect.closest('.collapse');
        if (collapseEl) {
            intervalSelect.addEventListener('change', function() {
                sessionStorage.setItem('openCollapse', collapseEl.id);
            });
        }
    }

    // Open collapse from sessionStorage (after form redirect)
    const openCollapse = sessionStorage.getItem('openCollapse');
    if (openCollapse) {
        sessionStorage.removeItem('openCollapse');
        const collapseEl = document.getElementById(openCollapse);
        if (collapseEl) {
            const bsCollapse = new bootstrap.Collapse(collapseEl, { show: true });
            // Header state will be updated by the show.bs.collapse event listener above
        }
    }
});
</script>
