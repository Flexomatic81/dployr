<div class="setup-card">
    <div class="setup-header text-center">
        <i class="bi bi-rocket-takeoff display-4"></i>
        <h1 class="mt-3 mb-0">Dployr Setup</h1>
        <p class="mb-0 opacity-75">Multi-User Hosting-Plattform</p>
    </div>

    <div class="card-body p-4">
        <!-- Step Indicator -->
        <div class="step-indicator" id="stepIndicator">
            <div class="step active" data-step="1">1</div>
            <div class="step" data-step="2">2</div>
            <div class="step" data-step="3">3</div>
        </div>

        <!-- Step 1: Server Configuration -->
        <div class="setup-step" id="step1">
            <h4 class="mb-4"><i class="bi bi-gear"></i> Server-Konfiguration</h4>

            <form id="configForm">
                <div class="mb-3">
                    <label for="server_ip" class="form-label">Server IP-Adresse</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-hdd-network"></i></span>
                        <input type="text" class="form-control" id="server_ip" name="server_ip"
                               value="<%= serverIp %>" required
                               pattern="^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$|^[a-zA-Z0-9.-]+$"
                               placeholder="192.168.1.100">
                    </div>
                    <div class="form-text">IP-Adresse oder Hostname des Servers</div>
                </div>

                <div class="mb-3">
                    <label for="mysql_root_password" class="form-label">MySQL Root Passwort</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-key"></i></span>
                        <input type="password" class="form-control" id="mysql_root_password" name="mysql_root_password"
                               required minlength="8" placeholder="Sicheres Passwort">
                        <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('mysql_root_password')">
                            <i class="bi bi-eye"></i>
                        </button>
                    </div>
                    <div class="form-text">Mindestens 8 Zeichen</div>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                        Weiter <i class="bi bi-arrow-right"></i>
                    </button>
                </div>
            </form>
        </div>

        <!-- Step 2: Admin Account -->
        <div class="setup-step d-none" id="step2">
            <h4 class="mb-4"><i class="bi bi-person-badge"></i> Admin-Konto erstellen</h4>

            <form id="adminForm">
                <div class="mb-3">
                    <label for="admin_username" class="form-label">Admin Benutzername</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-person"></i></span>
                        <input type="text" class="form-control" id="admin_username" name="admin_username"
                               required pattern="[a-z0-9_-]+" placeholder="admin">
                    </div>
                    <div class="form-text">Login-Name für das Dashboard</div>
                </div>

                <div class="mb-3">
                    <label for="admin_password" class="form-label">Admin Passwort</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-lock"></i></span>
                        <input type="password" class="form-control" id="admin_password" name="admin_password"
                               required minlength="6" placeholder="Passwort">
                        <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('admin_password')">
                            <i class="bi bi-eye"></i>
                        </button>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="admin_password_confirm" class="form-label">Passwort bestätigen</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                        <input type="password" class="form-control" id="admin_password_confirm"
                               required minlength="6" placeholder="Passwort wiederholen">
                    </div>
                </div>

                <div class="mb-3">
                    <label for="system_username" class="form-label">System-Benutzername</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-terminal"></i></span>
                        <input type="text" class="form-control" id="system_username" name="system_username"
                               required pattern="[a-z0-9_-]+" placeholder="admin">
                    </div>
                    <div class="form-text">Für Projekt-Verzeichnisse: /users/&lt;name&gt;/</div>
                </div>

                <div class="d-flex justify-content-between">
                    <button type="button" class="btn btn-outline-secondary" onclick="goToStep(1)">
                        <i class="bi bi-arrow-left"></i> Zurück
                    </button>
                    <button type="submit" class="btn btn-primary">
                        Installation starten <i class="bi bi-play-fill"></i>
                    </button>
                </div>
            </form>
        </div>

        <!-- Step 3: Installation Progress -->
        <div class="setup-step d-none" id="step3">
            <h4 class="mb-4"><i class="bi bi-download"></i> Installation</h4>

            <div id="progressSteps">
                <div class="progress-step pending" data-step="network">
                    <i class="bi bi-circle me-2"></i>
                    <span>Docker-Netzwerk prüfen</span>
                </div>
                <div class="progress-step pending" data-step="wait_db">
                    <i class="bi bi-circle me-2"></i>
                    <span>Auf Datenbank warten</span>
                </div>
                <div class="progress-step pending" data-step="dashboard_db">
                    <i class="bi bi-circle me-2"></i>
                    <span>Dashboard-Datenbank erstellen</span>
                </div>
                <div class="progress-step pending" data-step="admin">
                    <i class="bi bi-circle me-2"></i>
                    <span>Admin-Benutzer erstellen</span>
                </div>
            </div>

            <div id="setupResult" class="d-none mt-4">
                <div class="alert alert-success" id="successAlert">
                    <h5><i class="bi bi-check-circle"></i> Installation erfolgreich!</h5>
                    <p class="mb-0">Die Webserver-Infrastruktur wurde eingerichtet.</p>
                </div>
                <div class="alert alert-danger d-none" id="errorAlert">
                    <h5><i class="bi bi-exclamation-triangle"></i> Fehler bei der Installation</h5>
                    <p class="mb-0" id="errorMessage"></p>
                </div>
            </div>

            <div id="completedActions" class="d-none mt-4">
                <div class="card bg-light">
                    <div class="card-body">
                        <h6>Installierte Services:</h6>
                        <ul class="mb-3">
                            <li>MariaDB auf Port 3306</li>
                            <li>phpMyAdmin auf Port 8080</li>
                            <li>Dashboard auf Port 3000</li>
                        </ul>
                        <a href="/login" class="btn btn-success btn-lg w-100">
                            <i class="bi bi-box-arrow-in-right"></i> Zum Dashboard
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    let setupData = {};

    function togglePassword(id) {
        const input = document.getElementById(id);
        input.type = input.type === 'password' ? 'text' : 'password';
    }

    function goToStep(step) {
        // Alle Steps ausblenden
        document.querySelectorAll('.setup-step').forEach(el => el.classList.add('d-none'));

        // Gewünschten Step einblenden
        document.getElementById('step' + step).classList.remove('d-none');

        // Step Indicator aktualisieren
        document.querySelectorAll('.step').forEach(el => {
            const s = parseInt(el.dataset.step);
            el.classList.remove('active', 'done');
            if (s < step) el.classList.add('done');
            if (s === step) el.classList.add('active');
        });
    }

    // Step 1: Config Form
    document.getElementById('configForm').addEventListener('submit', function(e) {
        e.preventDefault();
        setupData.server_ip = document.getElementById('server_ip').value;
        setupData.mysql_root_password = document.getElementById('mysql_root_password').value;
        goToStep(2);
    });

    // Step 2: Admin Form
    document.getElementById('adminForm').addEventListener('submit', function(e) {
        e.preventDefault();

        const password = document.getElementById('admin_password').value;
        const confirm = document.getElementById('admin_password_confirm').value;

        if (password !== confirm) {
            alert('Passwörter stimmen nicht überein!');
            return;
        }

        setupData.admin_username = document.getElementById('admin_username').value;
        setupData.admin_password = password;
        setupData.system_username = document.getElementById('system_username').value;

        goToStep(3);
        runSetup();
    });

    async function runSetup() {
        const steps = ['network', 'wait_db', 'dashboard_db', 'admin'];

        // Alle Steps auf "pending" setzen
        steps.forEach(step => {
            updateStepStatus(step, 'pending');
        });

        try {
            // Schrittweise animieren
            for (let i = 0; i < steps.length; i++) {
                updateStepStatus(steps[i], 'running');
                await new Promise(r => setTimeout(r, 500));
            }

            // Echten API-Call machen
            const response = await fetch('/setup/run', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(setupData)
            });

            const result = await response.json();

            if (result.success) {
                // Alle Steps als erledigt markieren
                steps.forEach(step => updateStepStatus(step, 'done'));

                document.getElementById('setupResult').classList.remove('d-none');
                document.getElementById('successAlert').classList.remove('d-none');
                document.getElementById('errorAlert').classList.add('d-none');
                document.getElementById('completedActions').classList.remove('d-none');
            } else {
                throw new Error(result.error);
            }
        } catch (error) {
            document.getElementById('setupResult').classList.remove('d-none');
            document.getElementById('successAlert').classList.add('d-none');
            document.getElementById('errorAlert').classList.remove('d-none');
            document.getElementById('errorMessage').textContent = error.message;
        }
    }

    function updateStepStatus(stepName, status) {
        const stepEl = document.querySelector(`.progress-step[data-step="${stepName}"]`);
        if (!stepEl) return;

        stepEl.className = 'progress-step ' + status;

        const icon = stepEl.querySelector('i');
        if (status === 'pending') {
            icon.className = 'bi bi-circle me-2';
        } else if (status === 'running') {
            icon.className = 'bi bi-arrow-repeat me-2';
            icon.classList.add('spinner-border', 'spinner-border-sm');
        } else if (status === 'done') {
            icon.className = 'bi bi-check-circle-fill me-2';
        } else if (status === 'error') {
            icon.className = 'bi bi-x-circle-fill me-2';
        }
    }
</script>
