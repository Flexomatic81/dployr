# Dployr Konfiguration
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.example .env

# ============================================
# PFLICHT - Diese Werte müssen gesetzt werden
# ============================================

# MySQL/MariaDB Root Passwort (mindestens 8 Zeichen)
MYSQL_ROOT_PASSWORD=DeinSicheresPasswort123!

# PostgreSQL Root Passwort (mindestens 8 Zeichen)
POSTGRES_ROOT_PASSWORD=DeinSicheresPostgresPasswort123!

# pgAdmin Passwort
PGADMIN_PASSWORD=DeinPgAdminPasswort123!

# Session Secret für das Dashboard (generieren mit: openssl rand -base64 32)
SESSION_SECRET=

# ============================================
# OPTIONAL - Standardwerte werden verwendet
# ============================================

# Dashboard Datenbank (verwendet MariaDB)
DB_DATABASE=dashboard
DB_USERNAME=dashboard_user
DB_PASSWORD=

# Ports
DASHBOARD_PORT=3000
PHPMYADMIN_PORT=8080
PGADMIN_PORT=5050

# pgAdmin E-Mail (für Login)
PGADMIN_EMAIL=admin@local.dev

# Server IP (wird automatisch erkannt, nur setzen wenn nötig)
# SERVER_IP=192.168.1.100
